import { createSlice } from "@reduxjs/toolkit";

const profilePosts = createSlice({
    name:"profilePosts",
    initialState:null,
})

export default profilePosts.reducer;
export const {} = profilePosts.actions;